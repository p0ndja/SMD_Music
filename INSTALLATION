How to installed:
1) Choose the plugin version that's match with your server version like: If your server is 1.9 you need to use 1.9-1.9.2 version of plugin
2) Drop the jar into "/plugins/". I recommand to "restart" because it need to create the config file which record the player option for toggling
3) The plugin will generate the folder call "song" in /plugins/Music/'songs' with 5 songs for starter server [NOT AVALIBLE YET!. YOU NEED TO CREATE '/music/songs' BY YOURSELF, WE WILL UPDATE THIS SOON!]. You're allowed to add your own song by drop the "NBS song" [Generate out from Minecraft Noteblock Studio] which is using ".nbs" (Other isn't allow). If you install the new song, Please reload the server. [In the future, We will add '/musicadmin reload' for reloading the new song!] (The info in actionbar will display the "Song Name" and "Song Author". You can set it in Settings>Song Properties in Minecraft Noteblock Studio)
4) When the player join the server, The player will hear the song by default. They need to using /music for toggling to mute, Type again to unmute. The song will be automatic suffle and playing the new song when the first one end.
**Note: admin can play the selected song from folder see your song by '/musicadmin list'

===========[For Developer]===========
I seperated the version because of "ActionBar".
I using "import net.minecraft.server.<VERSION>...." in action bar that need to change every time from the import server.
1.9 , 1.10 and 1.11 are using the same code just ActionBar is different.
But 1.8 is using the different code about the "Sound" because in 1.9 some of them are changing to new name.

**The Latest version of plugin for the newest game server [Like 1.9 , 1.10 , 1.11 etc.] will always up-to-date and the older version will come later**

ที่ต้องแยกเป็น Version ย่อยๆ  เพราะเจ้าระบบ ActionBar
มันจะต้องแก้ตัว Import ทุกครั้งทีเปลี่ยน Version ของ Java Build Path ใน Properties สำหรับเป็น Library
ตัว 1.9 - 1.11 เนี่ย ใช้โค้ตเหมือนกันยกเว้นส่วนของ ActionBar ซึ่งถ้าอยากนำไปพัฒนา แนะนำให้ใช้ตัวที่เป็น Version ล่าสุดที่มีของเกม เช่น 1.9 , 1.10 , 1.11 ตัว 1.11 จะ Update มากที่สุด
ส่วน 1.8 จะมีเรื่องของ Sound ที่แตกต่างกันด้วย เพราะบางอันมันถูกเปลี่ยนชื่อ

**ตัวที่ล่าสุดของปลักอิน จะอัพตาม Version ล่าสุดของเกม เช่น 1.11 มาใหม่สุด Plugin ของ 1.11 จะ Update เป็นปัจจุบันมากที่สุด แล้ว Version อื่น ๆ จะตามมา**
